/**
 * Plugins Barrel Export
 *
 * Re-exports all framework integration plugins.
 *
 * - {@link viteConfigPlugin} — Vite plugin that serves config via virtual module with HMR
 *
 * @module plugins
 */

export { viteConfigPlugin } from './vite.plugin';
